services:
  foundercopilot:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - ./data:/app/data
      - ./state:/app/state
  redis:
    image: redis:7-alpine
    ports:
      - "6380:6379"